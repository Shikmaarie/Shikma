# רחלי חדד — אתר ראשי + חנות

אתר תדמית וחנות איקומרס לרחלי חדד, מנטורית לעסקים וכסף.
Next.js 15 (App Router), React 19, TypeScript, Tailwind CSS v4, ו-Three.js
לאנימציית ההירו התלת-ממדית. סליקה דרך **קארדקום**.

---

## הפעלה מקומית

```bash
npm install
cp .env.example .env.local   # ומלאו את הערכים
npm run dev                  # http://localhost:3000
```

בנייה לפרודקשן:

```bash
npm run build && npm start
```

## משתני סביבה

| משתנה | תיאור |
| --- | --- |
| `CARDCOM_TERMINAL` | מספר המסוף מהמערכת של קארדקום |
| `CARDCOM_API_NAME` | שם משתמש ה-API מקארדקום |
| `NEXT_PUBLIC_SITE_URL` | הכתובת הציבורית של האתר (חייבת להתאים לדומיין החי) |
| `REGISTRATIONS_ADMIN_PASSWORD` | סיסמה לעמוד `/rishum/admin` — צפייה בהרשמות והורדת האקסל |
| `KV_REST_API_URL` | כתובת ה-REST של מאגר Vercel KV / Upstash לשמירת ההרשמות |
| `KV_REST_API_TOKEN` | הטוקן של אותו מאגר |

את הערכים האמיתיים מגדירים בשרת/בפאנל האחסון — **לא** בקוד ולא ב-git.
ללא הגדרתם האתר עולה כרגיל, ורק כפתור התשלום מחזיר שגיאה מנומסת.

---

## מבנה הפרויקט

```
src/
├── app/
│   ├── page.tsx                     עמוד הבית
│   ├── about/                       אודות
│   ├── programs/                    תוכניות עסקיות
│   ├── wealth/                      שפע ומיינדסט
│   ├── club/                        מועדון עסקים
│   ├── books/                       ספרים ומדריכים
│   ├── contact/                     צור קשר
│   ├── store/                       החנות ועמודי המוצר
│   ├── checkout/                    עגלה, טופס, הצלחה/כישלון
│   ├── legal/[doc]/                 תקנון, פרטיות, נגישות
│   ├── rishum/                      טופס רישום הילדים + עמוד הניהול
│   └── api/
│       ├── checkout/                פתיחת עסקה מול קארדקום
│       ├── registrations/           קליטת הרשמות וייצוא לאקסל
│       └── cardcom/webhook/         אישור תשלום שרת-לשרת
├── components/
│   ├── three/                       סצנת ה-DNA התלת-ממדית
│   ├── sections/                    סקשנים של עמוד הבית
│   ├── store/                       כרטיס מוצר, עגלה, הוספה לעגלה
│   └── ui/                          רכיבים משותפים
├── data/
│   ├── site.ts                      כל הטקסטים של האתר
│   └── products.ts                  קטלוג המוצרים והמחירים
└── lib/
    ├── cardcom.ts                   אינטגרציית הסליקה
    ├── cart.ts                      ניהול העגלה
    ├── registration-fields.ts       שדות טופס הרישום (משותף לדפדפן ולשרת)
    ├── registrations.ts             ולידציה, אחסון ובניית האקסל
    └── xlsx.ts                      כתיבת קובצי .xlsx בלי ספריות חיצוניות
```

### עריכת תוכן

כל הטקסטים והמחירים יושבים ב-`src/data/`. לשינוי מחיר, שם תוכנית או
טקסט בעמוד הבית — עורכים שם בלבד, בלי לגעת ברכיבים.

### שלושת מצבי המוצר

לכל מוצר ב-`products.ts` יש `mode` שקובע איך רוכשים אותו:

| mode | התנהגות |
| --- | --- |
| `purchase` | יש מחיר מפורסם — נכנס לעגלה ועובר לסליקת קארדקום |
| `application` | ליווי במחיר לא מפורסם — הכפתור מוביל לשיחת התאמה, לא לעגלה |
| `free` | מדריך או הדרכה חינמית — הרשמה בלבד, לעולם לא נכנס לעגלה |

כרגע כל תוכניות הליווי הגדולות מוגדרות `application`, כי לא פורסם להן מחיר.
**כדי להפוך תוכנית למכירה ישירה** מוסיפים לה `price` ומשנים את ה-`mode`
ל-`purchase` — שאר האתר מתעדכן לבד. גם השרת אוכף את זה: ניסיון לשלוח מוצר
שאינו `purchase` לסליקה נדחה.

---

## טופס רישום הילדים (`/rishum`)

טופס עברי נפרד, לשליחה להורים בוואטסאפ. הוא לא חלק מהאתר השיווקי: הכותרת,
התפריט והפוטר של רחלי לא מוצגים בו (`ChromeGate`), והוא מסומן `noindex` כדי
שלא ייכנס לגוגל.

**הצבעים שלו בהירים וילדותיים** — קרם, טורקיז בהיר, שמש, לילך ואלמוג — ולא
הטורקיז הכהה והזהב של האתר. הטוקנים יושבים ב-`globals.css` בקידומת `kid-`
ומשמשים **רק** את הדפים האלה; שאר האתר לא מושפע.

**מה ההורה ממלא:** פרטי ההורה פעם אחת (שם + טלפון), ואחר כך שורה לכל ילד/ה —
שם ושם משפחה, מין, כיתה (גן עד י״ב) ושם הגן או בית הספר. אפשר להוסיף עד 12
ילדים בטופס אחד; שם בית הספר של הילד/ה הקודם/ת מועתק אוטומטית לשורה הבאה.

**חכם במה:**

- טיוטה נשמרת בדפדפן — שיחה באמצע המילוי לא מוחקת את מה שנכתב.
- מספר הטלפון מתקבל בכל צורה (`+972`, רווחים, מקפים) ונשמר אחיד: `050-1234567`.
- שגיאות מוצגות ליד השדה עצמו, בעברית, לפני השליחה.
- הרשמה כפולה של אותו ילד/ה מאותו טלפון לא יוצרת שורה נוספת.
- שדה מלכודת (honeypot) חוסם בוטים בלי CAPTCHA.

### איפה הנתונים נשמרים

| סביבה | אחסון |
| --- | --- |
| Vercel / ייצור | Vercel KV (Upstash Redis) — `KV_REST_API_URL` + `KV_REST_API_TOKEN`. ההוספה אטומית, כך ששני הורים ששולחים באותה שנייה לא דורסים זה את זה |
| מקומי / שרת משלנו | `data/registrations.jsonl` וגם `data/registrations.xlsx` שנכתב מחדש אחרי כל הרשמה. התיקייה ב-`.gitignore` |

בלי משתני ה-KV הטופס נופל לאחסון המקומי — מה שלא שורד ב-Vercel, כי הדיסק שם
נמחק בין הרצות. **לפני שליחת הלינק להורים יש להגדיר את שני המשתנים.**

### הורדת האקסל

`/rishum/admin` → סיסמה (`REGISTRATIONS_ADMIN_PASSWORD`) → טבלה של כל
ההרשמות וכפתור **הורדת האקסל**. הקובץ נבנה מחדש בכל לחיצה, כך שהוא תמיד
מעודכן.

העמודות זהות לקובץ המקורי (`טבלת רישום ילדים ונוער מגילאי גן עד יב'`), לפי
אותו סדר: טלפון ההורה · שם אחד ההורים · שם הגן או בית הספר · כיתה · מין ·
שם ושם משפחה של הילד/ה. נוספה עמודה אחת — **תאריך ההרשמה**.

שני הבדלים מכוונים מול הקובץ המקורי: הטלפון נשמר כטקסט ולא כמספר (בקובץ
המקורי האפס בהתחלה נעלם), והגיליון מתחיל ישר בשורת הכותרות — בלי שורת
הכותרת והוראות שמעליה — כדי שהמיון והסינון של Excel יעבדו.

`src/lib/xlsx.ts` כותב את הקובץ ישירות: .xlsx הוא zip של XML, וזה חוסך
ספריית ספרדשיטים שלמה בשביל גיליון אחד.

---

## תהליך התשלום

1. הלקוחה מוסיפה מוצרים לעגלה (נשמרת ב-`localStorage`).
2. בטופס התשלום נשלחים לשרת רק **מזהי המוצרים והכמויות**.
3. השרת מתמחר מחדש כל שורה מהקטלוג — **סכום שנשלח מהדפדפן לא נלקח בחשבון**,
   כך שלא ניתן לשנות מחיר מצד הלקוח.
4. השרת פותח עסקת LowProfile מול קארדקום ומחזיר כתובת לעמוד הסליקה.
5. קארדקום מחזירה את הלקוחה ל-`/checkout/success` או `/checkout/failed`,
   ובמקביל קוראת לשרת ל-`/api/cardcom/webhook`.
6. ה-webhook **מאמת מול קארדקום** את מצב העסקה לפני שהוא מסמן אותה כשולמה,
   כך שקריאה מזויפת לא יכולה לאשר הזמנה.

### מה נשאר לחבר לפני עלייה לאוויר

- [ ] להזין `CARDCOM_TERMINAL` ו-`CARDCOM_API_NAME` בשרת.
- [ ] להשלים את `fulfilOrder` ב-`src/app/api/cardcom/webhook/route.ts` —
      מתן גישה לתכנים, הוספה ל-CRM/דיוור ושליחת מייל פתיחה.
- [ ] **להחליף את פרטי הקשר** ב-`src/data/site.ts` — הטלפון, מספר הוואטסאפ
      וכתובות הרשתות החברתיות הם כרגע ערכי מקום ולא מספרים אמיתיים.
- [ ] להחליט אם לפרסם מחירים לתוכניות הליווי (ראו „שלושת מצבי המוצר”).
- [ ] להעביר את שלושת מסמכי ה-`legal` לעריכת עורך דין — הנוסח הנוכחי טיוטה.
- [ ] לוודא ששמות המשתתפות בהמלצות מאושרים לפרסום.
- [ ] להתקין את סקריפט „נגיש בקליק” — הצהרת הנגישות בפוטר כבר מפנה אליו,
      אבל הסקריפט עצמו עוד לא מוטמע ב-`layout.tsx`.
- [ ] להוסיף קישור ישיר לעמוד הספר באמזון (מוזכר בטקסט, אין עדיין URL).
- [ ] **פרטי משפחה** — שמות בן הזוג והילדים הוסרו במכוון מכל הטקסטים
      (במסמך המקור התערבבו שמות שאינם של רחלי). מספר הילדים גם הוא לא
      מצוין. להוסיף רק אם רחלי מאשרת במפורש מה לפרסם.

---

## עיצוב: אמנות הכיסוי של המוצרים

אין תמונות של המוצרים, ותמונות סטוק היו נראות כמו מילוי מקום. במקום זאת כל
מוצר מקבל „סיגיל” מצויר ב-`ProductSigil.tsx` — מוטיב לפי קטגוריה, שמשתנה
דטרמיניסטית לפי ה-slug:

| קטגוריה | מוטיב |
| --- | --- |
| תוכניות עסקיות | עמודות עולות — צמיחה וסקייל |
| שפע ומיינדסט | מעגלים מתפשטים — עבודה פנימית שמתפשטת חוצה |
| מועדון עסקים | עיגולים משתלבים — הקהילה |
| ספרים ומדריכים | דפים נפרשים |

הכל דטרמיניסטי בכוונה: אותו מוצר מקבל אותו ציור בכל build, בשרת ובדפדפן,
כך שאין הבדל בין הרינדור בצד השרת לצד הלקוח.

## תמונת השיתוף (OG) וטקסט עברי

`src/app/opengraph-image.tsx` מייצר את תמונת השיתוף לרשתות. שני דברים שם
נראים מוזרים אבל הכרחיים:

1. **הפונט נקרא מהריפו** (`src/app/_assets/heebo-800.ttf`) ולא מגוגל.
   fetch חי שם היה גורם לתצוגות המקדימות להישבר בשקט בכל תקלת רשת.
2. **כל טקסט עברי עובר דרך `visual()`** מ-`src/lib/bidi.ts`. המנוע שמרנדר
   את התמונה (Satori) לא מיישם את אלגוריתם ה-bidi של יוניקוד, ובלי הסידור
   הזה העברית יוצאת הפוכה. השורות מפוצלות ביד ומסומנות `nowrap` — גלישה
   אוטומטית של טקסט שכבר סודר חזותית תשבור את סדר הקריאה.

`src/lib/bidi.ts` הוא **רק לתמונות**. אסור להשתמש בו ב-HTML — דפדפנים
מיישמים bidi נכון, וטקסט הפוך שם ישבור בחירה, חיפוש וקוראי מסך.

## נגישות וביצועים

- כיוון `rtl` ושפה `he` ברמת המסמך, טיפוגרפיה עברית (Heebo + Frank Ruhl Libre).
- ניווט מלא במקלדת עם סימון מיקוד גלוי, דילוג לתוכן, ותוויות ARIA בעברית.
- `prefers-reduced-motion` מכבה את כל האנימציות **ואת הסצנה התלת-ממדית**,
  ובמקומה מוצג רקע סטטי.
- הסצנה נטענת ב-`dynamic import` ללא SSR, יורדת לאיכות נמוכה במובייל ובמכשירים
  חלשים, ולא נטענת כלל ללא WebGL.
- כל הגופים בסצנה מרונדרים ב-instancing — הסצנה כולה היא מספר קטן של draw calls.
